/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * Api
 * GymMice API specification.
 *
 * The server assigns a new UUID request ID to every request and returns it in
 * `X-Request-Id`, including parser and error responses. Incoming request IDs
 * are not trusted or reused.
 *
 * API failures use `ApiErrorResponse`. The infrastructure error codes defined
 * here cover malformed requests (400), oversized payloads (413), unsupported
 * media types or encodings (415), schema validation failures (422), unknown
 * routes (404), unexpected failures (500), and temporary service
 * unavailability (503). Authentication, authorization, conflict, and rate
 * limit behavior (401, 403, 409, and 429) belongs to later endpoint work and
 * is not implemented by this contract.
 *
 * JSON and URL-encoded request bodies are limited to 100 KiB. URL-encoded
 * bodies are additionally limited to 100 parameters and a nesting depth of 5.
 *
 * OpenAPI spec version: 0.2.0
 */
export interface HealthStatus {
  status: string;
}

/**
 * Stable machine-readable infrastructure error code.
 */
export type ApiErrorCode = (typeof ApiErrorCode)[keyof typeof ApiErrorCode];

export const ApiErrorCode = {
  INVALID_REQUEST: "INVALID_REQUEST",
  INVALID_JSON: "INVALID_JSON",
  PAYLOAD_TOO_LARGE: "PAYLOAD_TOO_LARGE",
  UNSUPPORTED_MEDIA_TYPE: "UNSUPPORTED_MEDIA_TYPE",
  VALIDATION_ERROR: "VALIDATION_ERROR",
  NOT_FOUND: "NOT_FOUND",
  INTERNAL_ERROR: "INTERNAL_ERROR",
  SERVICE_UNAVAILABLE: "SERVICE_UNAVAILABLE",
} as const;

/**
 * Safe field metadata; submitted values are never included.
 */
export interface ValidationErrorDetail {
  field: string;
  code: string;
}

export interface ApiError {
  code: ApiErrorCode;
  /** Safe user-facing message without internal diagnostics. */
  message: string;
  requestId: string;
  /** Optional safe field-validation metadata. */
  details?: ValidationErrorDetail[];
}

export interface ApiErrorResponse {
  error: ApiError;
}

/**
 * Standard API infrastructure failure.
 */
export type StandardErrorResponse = ApiErrorResponse;

/**
 * The request is malformed.
 */
export type InvalidRequestResponse = ApiErrorResponse;

/**
 * The request body exceeds the accepted size.
 */
export type PayloadTooLargeResponse = ApiErrorResponse;

/**
 * The request media type or content encoding is unsupported.
 */
export type UnsupportedMediaTypeResponse = ApiErrorResponse;

/**
 * A well-formed request fails schema validation.
 */
export type ValidationFailedResponse = ApiErrorResponse;

/**
 * No route or resource matches the request.
 */
export type NotFoundResponse = ApiErrorResponse;

/**
 * An unexpected server failure occurred.
 */
export type InternalErrorResponse = ApiErrorResponse;

/**
 * The service is temporarily unavailable.
 */
export type ServiceUnavailableResponse = ApiErrorResponse;
